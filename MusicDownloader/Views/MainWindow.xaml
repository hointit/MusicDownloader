<Window x:Class="MusicDownloader.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MusicDownloader.Views"
        xmlns:viewModel="clr-namespace:MusicDownloader.ViewModels"
        mc:Ignorable="d"
        Title="MainWindow" Height="800" Width="600">
    
    <Window.DataContext>
        <viewModel:MainViewModel/>
    </Window.DataContext>
    
    <StackPanel Margin="20">
        <TextBox Text="{Binding Playlist, Mode=TwoWay}"></TextBox>
        <Button Command="{Binding LoadPlayListCommand}" Width="200" Margin="0 10 0 0">Load Play List</Button>
        <DataGrid Margin="0 10 0 0" Name="dg" BorderThickness="1" Height="600" CanUserAddRows="False" AutoGenerateColumns="False" ItemsSource="{Binding ListSong}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="File" Width="*" Binding="{Binding Name}"></DataGridTextColumn>
                <DataGridTextColumn Header="Status" Width="150" Binding="{Binding Status}"></DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>
        <Button Command="{Binding DownLoadPlayListCommand}" Width="200" Margin="0 10 0 0">DownLoad List</Button>
    </StackPanel>
</Window>
